# SPDX-FileCopyrightText: 2022 - Agency for Digital Government
#
# SPDX-License-Identifier: CC0-1.0

# Gitleaks configuration for secret scanning

[extend]
# Use default gitleaks rules
useDefault = true

# Allowlist for documentation containing example curl commands with placeholder tokens
[allowlist]
description = "Ignore files with placeholder tokens in documentation and tests"
paths = [
  # Documentation with example curl commands using GITHUB_TOKEN as a placeholder (not a real token)
  '''docs/verification\.md''',
  # Test file for CI scrub function using fake tokens to verify scrubbing works
  '''tests/ci/scrub\.bats'''
]
